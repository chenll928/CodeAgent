# IntentGraph 快速参考指南

## 快速开始

### 安装
```bash
pip install intentgraph
```

### 基础命令

```bash
# 分析当前目录
intentgraph .

# 生成聚类分析
intentgraph . --cluster

# 指定输出级别
intentgraph . --level minimal    # ~10KB (默认)
intentgraph . --level medium     # ~70KB
intentgraph . --level full       # ~340KB

# 指定输出文件
intentgraph . --output analysis.json

# 显示依赖循环
intentgraph . --show-cycles
```

## AI 原生接口

### 连接代码库
```python
from intentgraph import connect_to_codebase

agent = connect_to_codebase("/path/to/repo", {
    "task": "bug_fixing",           # 任务类型
    "token_budget": 30000,          # Token 预算
    "agent_type": "code_reviewer"   # 代理类型
})
```

### 自然语言查询
```python
# 查询高复杂度文件
results = agent.query("查找高复杂度的文件")

# 探索安全模式
insights = agent.explore("security")

# 获取推荐操作
recommendations = agent.recommend_next_actions()
```

### 能力发现
```python
from intentgraph import get_capabilities_manifest

capabilities = get_capabilities_manifest()
analysis_types = capabilities["capabilities"]["analysis_types"]
```

## 任务类型

| 任务 | 代码 | 适用场景 |
|------|------|---------|
| 错误修复 | `bug_fixing` | 查找和修复 bug |
| 功能开发 | `feature_development` | 开发新功能 |
| 代码审查 | `code_review` | 代码质量审查 |
| 安全审计 | `security_audit` | 安全漏洞检测 |
| 重构 | `refactoring` | 代码重构 |
| 文档生成 | `documentation` | 生成文档 |
| 测试 | `testing` | 编写测试 |
| 性能优化 | `performance_optimization` | 性能改进 |
| 依赖分析 | `dependency_analysis` | 依赖关系分析 |

## 聚类模式

### 启用聚类
```bash
# 分析模式（依赖分析）
intentgraph . --cluster --cluster-mode analysis

# 重构模式（功能分组）
intentgraph . --cluster --cluster-mode refactoring

# 导航模式（大小优化）
intentgraph . --cluster --cluster-mode navigation
```

### 聚类配置
```bash
# 指定聚类大小
intentgraph . --cluster --cluster-size 10KB
intentgraph . --cluster --cluster-size 15KB  # 默认
intentgraph . --cluster --cluster-size 20KB

# 指定索引级别
intentgraph . --cluster --index-level basic  # 简单映射
intentgraph . --cluster --index-level rich   # 完整元数据（默认）
```

## 输出级别对比

| 级别 | 大小 | 适用场景 | 包含内容 |
|------|------|---------|---------|
| **minimal** | ~10KB | AI 代理、快速分析 | 路径、依赖、导入、基础指标 |
| **medium** | ~70KB | 详细分析、代码审查 | + 关键符号、导出、可维护性分数 |
| **full** | ~340KB | 全面审计 | 完整分析和所有元数据 |

## 编程接口

### 基础分析
```python
from intentgraph import RepositoryAnalyzer

analyzer = RepositoryAnalyzer(
    language_filter=['python'],
    include_tests=False,
    workers=4
)

result = analyzer.analyze('/path/to/repo')
print(f"找到 {len(result.files)} 个文件")
```

### 任务特定代理
```python
# 错误修复代理
bug_agent = connect_to_codebase("/path/to/repo", {
    "task": "bug_fixing",
    "token_budget": 20000
})
issues = bug_agent.query("查找潜在问题区域")

# 安全审计代理
security_agent = connect_to_codebase("/path/to/repo", {
    "task": "security_audit",
    "token_budget": 40000
})
vulnerabilities = security_agent.query("查找输入验证问题")
```

## 常用命令组合

### CI/CD 集成
```bash
intentgraph . --level minimal --format compact --output analysis.json
```

### 代码审查
```bash
intentgraph . --level medium --show-cycles
```

### 全面审计
```bash
intentgraph . --level full --include-tests --output audit.json
```

### 大型代码库
```bash
intentgraph . --cluster --cluster-mode navigation --cluster-size 15KB
```

## 配置建议

### .gitignore
```bash
# 添加到 .gitignore
echo ".intentgraph/" >> .gitignore
```

### 并行处理
```bash
# 使用多个工作进程
intentgraph . --workers 8
```

### 语言过滤
```bash
# 只分析特定语言
intentgraph . --lang py,js,ts
```

## 输出结构

### 分析结果
```json
{
  "files": [{
    "path": "src/models.py",
    "language": "python",
    "symbols": [...],
    "function_dependencies": [...],
    "complexity_score": 4,
    "maintainability_index": 82.5
  }],
  "summary": {
    "total_files": 42,
    "total_functions": 156
  }
}
```

### 聚类索引
```json
{
  "clusters": [{
    "cluster_id": "domain",
    "name": "Domain Layer",
    "file_count": 5,
    "total_size_kb": 12.4
  }],
  "cluster_recommendations": {
    "finding_bugs": ["domain", "utilities"]
  }
}
```

## 性能参考

| 仓库大小 | 文件数 | 分析时间 |
|---------|-------|---------|
| 小型 | < 50 | ~2s |
| 中型 | < 500 | ~12s |
| 大型 | < 2000 | ~45s |

## 支持的语言

- ✅ **Python**: 完全支持（函数、类、装饰器、复杂度）
- 🔶 **JavaScript**: 基础支持（文件级依赖）
- 🔶 **TypeScript**: 基础支持（文件级依赖）
- 🔶 **Go**: 基础支持（文件级依赖）

## 资源链接

- **GitHub**: https://github.com/Raytracer76/intentgraph
- **文档**: [docs/](docs/)
- **示例**: [examples/](examples/)
- **问题**: https://github.com/Raytracer76/intentgraph/issues

